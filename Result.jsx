/*
Result should be displayed when you search to confirm if this is the movie the user wants to find
    There should be a Button to add this one movie to state.
    Perhaps on tap or some event listener, the app will display Display.jsx, which should return the deliverables
*/

import React from 'react'
import {StyleSheet, View, Button, Text} from 'react-native'

const styles = StyleSheet.create({
    movieImage: {
        width: 20,
        height: 30
    }
})

export default function Result(props){
    return(
        
    )
}
